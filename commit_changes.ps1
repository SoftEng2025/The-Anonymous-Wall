
# Commit postController.js
git add src/backend/controllers/postController.js
git commit -m "fix: Resolve syntax error and add validation in postController"

# Commit messageController.js
git add src/backend/controllers/messageController.js
git commit -m "fix: Add ID validation and type safety in messageController"

# Commit AdminDashboard.jsx
git add src/pages/AdminDashboard/AdminDashboard.jsx
git commit -m "fix: Handle invalid IDs and type errors in AdminDashboard"

# Commit Forum.jsx
git add src/pages/Forum/Forum.jsx
git commit -m "fix: Ensure robust ID handling for saving posts in Forum"

# Push changes
git push origin main
